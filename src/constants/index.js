const { API_HOST } = process.env;
const { NODE_ENV } = process.env;
const { INIT_TIMEOUT } = process.env;
const { DEFAULT_BOT_NAME } = process.env;

export {
    API_HOST,
    NODE_ENV,
    INIT_TIMEOUT,
    DEFAULT_BOT_NAME,
};
